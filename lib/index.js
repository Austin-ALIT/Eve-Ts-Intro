"use strict";
let message = 'Hello world';
// let arrNum: Array<number> = [1,2,3];
let arrNum = [1, 2, 3];
let sett = new Set([1, 2, 3]); // Sets cannot contain duplicates
// note: primitive types are lowercase. 
// Instance types start with a capital (such as Array type)
message += " again";
console.log(message);
